import React from 'react';
import styled from 'styled-components';

const Home = () => {
    return (
        <Main>
            
        </Main>
    );
};

export default Home;

const Main = styled.div`
    display: flex;
`